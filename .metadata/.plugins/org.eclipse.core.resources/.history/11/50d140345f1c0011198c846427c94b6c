package basics;
class Book{
	String title;
	boolean available;
	double price;
	
	Book(String title) {
		this.title = title;
		this.available =true;
		
		if(this.price<0) {
			throw new priceException("invalid price");
		}
	}
}